package usecase

import (
	"testing"
)

// The use case method makes only one call to the repository and returns the result.
// There is no need for tests.
func TestAccountUseCase_ListAccounts(t *testing.T) {}
